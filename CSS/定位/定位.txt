1.为什么需要定位？
    1.某个元素可以自由的在一个盒子内移动位置，并且压住其他盒子。
    2.滚动窗口时，有一些模块是固定的。
2.定位
    将盒子定在某一个位置。
    定位 = 定位模式 + 边偏移。
    定位模式用于指定一个元素在文档中的定位方式。边偏移则决定了该元素的最终位置。
3.定位模式
    position:static(relative,absolute,fixed,sticky);
4.边偏倚
    1.top 2.bottom 3.left 4.right 专门用于定位。